/**
 * Roof MRI — GPS Scanning
 * 2D flat-roof measurement with the Emlid Reach RX2 RTK rover.
 */

import React from 'react';
import { Root } from './src/ui/Root';

function App(): React.JSX.Element {
  return <Root />;
}

export default App;
